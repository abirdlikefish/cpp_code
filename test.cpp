#include <bits/stdc++.h>
using namespace std;

int main()
{

    double a,b;
    cin >> a >> b  ;
    cout << (a - b) / b << endl;

    // double t1,d1,t2,d2,D,M,L;
    // cin >> t1 >> d1 >> t2 >> d2 ;

    // M = 220.97;
    // D = 0.0975;
    // L = 45.73;

    // t1 /= 5;
    // d1 /=4;
    // t2 /= 5;
    // d2 /=4;

    // cout << 128.0 * acos(-1) * L * M * (d1*d1 - d2 * d2)/D/D/D/D/(t1*t1 - t2*t2) / 10 << endl;

    // double a,b;
    // cin >> a >> b ;
    // cout<< sqrt ( a * a * 1.14 + b * b )<<endl;


    // double avg;
    // double num[10];
    // cin>>avg;
    // for(int i=0; i<5; i++)
    //     cin>>num[i];
    // double sum = 0;
    // for(int i=0; i<5; i++)
    //     sum += (num[i]-avg) * (num[i]-avg);
    // sum /= 20;
    // cout << sqrt(sum) << endl;


    // cout<< 4.0 * acos(-1) * acos(-1) * ( 26.72 + 1.0014) / (60 + 3  + 26.2 / 60) * 60 / (60 + 3 + 26.2 / 60) * 60 ;
    return 0;
}